@model System.Collections.Generic.IEnumerable<Bidia.Models.BidiaModels.ItemModel>

@{
	ViewBag.Title = "Index";
}
<div class="row">
	<div class="col-md-12">
		<br />
		<p class="header" style="font-weight: 700;font-family: calibri;font-size: 16px;">
			<strong>
				One of the finest different type enterprise.
				Here we make our own clothing, apparel and home decors.
				As well as we export and import dresses. BiDia always care about your choiceable products.
				Here you can find cloths for both men, women and children and you can even find various necessary items for your home.
				BiDia is 100% reliable corporation.
			</strong>
		</p>
	</div>
</div>
<hr />
<div class="well well-sm">
	<marquee><a style="color:#8b0000;font-size: 16px">New Arrivals</a></marquee>
</div>
<div id="myCarousel" class="carousel row" data-ride="carousel">
	<!-- Indicators -->
	@*<ol class="carousel-indicators">
		<li data-target="#myCarousel" data-slide-to="0" class="active"></li>
		<li data-target="#myCarousel" data-slide-to="1"></li>
		<li data-target="#myCarousel" data-slide-to="2"></li>
		<li data-target="#myCarousel" data-slide-to="3"></li>
	</ol>*@

	<!-- Wrapper for slides -->
	<div class="carousel-inner" role="listbox" style="text-align: center">
		@foreach (var i in Model)
		{
			<div class="item active">
				<img src="~/Pictures/@(i.Name).png">
				<div class="carousel-caption">
					<h3>@i.Name</h3>
					<p>@i.ItemDescription</p>
				</div>
			</div>
		}

	</div>

	<!-- Left and right controls -->
	<a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
		<span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
		<span class="sr-only">Previous</span>
	</a>
	<a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
		<span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
		<span class="sr-only">Next</span>
	</a>
</div>

<style>
	.carousel-inner > .item > img,
	.carousel-inner > .item > a > img {
		width: 70%;
		margin: auto;
	}

	.carousel-control.right {
		top: 50%;
	}

	.carousel-control.left {
		top: 50%;
	}
</style>

@*@section row1 {
		<div class="well well-sm" > <marquee > <a style="color:#8b0000;font-size: 16px" > New Arrivals</a > </marquee > </div > @foreach (var item in Model) {
			@Html.Action("HomeItempartialview", new { id = item.Id });
		}
	}

*@
@section scripts{
	<script src="~/Scripts/bootstrap.min.js"></script>
}